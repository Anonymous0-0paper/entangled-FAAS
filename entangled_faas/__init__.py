"""
Entangled-FaaS — Discrete-Event Simulator Package
===================================================
Modules:
  config    — Hyperparameters and constants
  sim_env   — QPU, ClassicalCloud, QuantumCloud
  scheduler — Priority queue + calibration-aware routing
  workload  — VQEJob, QuantumFuture, BackgroundBatchJobGenerator
  tracker   — MetricsTracker: TTNS, QDC, convergence, drift penalty
  main      — Run both baselines and print comparative results
"""
